# Uncomment and set to use external storage for ADS-B history
# PGDATA_PATH=/mnt/external/intercept/pgdata

# GitHub API token (optional). Helps avoid rate limits for update checks.
# Create a Fine-grained PAT or classic token with read access to repo metadata.
GITHUB_TOKEN=
